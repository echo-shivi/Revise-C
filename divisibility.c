#include <stdio.h>
int main()
{
    // int n;
    // scanf("%d", &n);
    // if (n % 2 == 0)
    // {
    //     printf("divisible by 2");
    // }
    // else
    // {
    //     printf("Not divisible by 2");
    // }

    // int a = 8 ^ 8;
    // printf("%d", a);

    // int isSunday = 1;
    // int isSnowing = 1;
    // printf("%d\n", isSnowing && isSunday);

    // int x;
    // printf("enter number:");
    // scanf("%d", &x);
    // printf("%d\n", x > 9 && x < 100);

    // Ternary operator
    // Condition ?do something if True: doSomething if false;
    // int age;
    // scanf("%d", &age);
    // age >= 18 && age <= 65 ? printf("vote") : printf("cant vote");



    
    return 0;
}