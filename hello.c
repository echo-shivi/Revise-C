#include <stdio.h>
#include <math.h>

int main()
{
    // printf("hello C");
    // Type declaration------------------------
    // int a, b, c, d, e = 0;
    // // e = 0;
    // printf("%d", e);
    //  int x=y=z=w=4; incorrect => use aur declare saath m nhi kar sakte phele humesha declare karna hoga phir use
    /*Arithmetic instruction--------------------------------------------------------------------------------------------------------
    
    -implicit jo khud se karta h aur explicit jo hum karte h
    
    int a = (int)1.999999;
    printf("%d", a);*/

    // operator precdence
    // *,/,% ->1st
    //+,- ->2nd
    //=
    // associativity rule(for same precedence)
    // agar same precedence h toh laga deinge left associativity

    //CONTROL iNSTRUCTIONS-----------------------------------------------


    return 0;
}